# Dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Build output — Railway builds this fresh on deploy
frontend/dist/

# Environment files — NEVER commit these
backend/.env
.env

# Database — Railway uses ephemeral storage; DB rebuilds on each deploy
backend/data/
data/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.log
npm-debug.log*
